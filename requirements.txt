neo4j-graphrag[openai,fuzzy-matching]>=1.18.0
neo4j>=5.28
pyyaml>=6.0

# PDF extraction. pdfplumber and pypdf are pure Python; the default poppler
# engine uses the pdftotext/pdffonts CLIs (apt install poppler-utils).
pdfplumber>=0.11
pypdf>=5.0

# Local embeddings, so a Claude-only setup needs no second API vendor.
neo4j-graphrag[anthropic,sentence-transformers]>=1.18.0
